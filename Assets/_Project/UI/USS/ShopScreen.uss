/* ═══════════════════════════════════════════
   SHOP SCREEN  —  ShopScreen.uss
   Colors match MainLayout.uss dark theme
═══════════════════════════════════════════ */

/* ── HEADER ── */
.shop-header {
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
    padding: 30px 36px 18px 36px;
    background-color: rgb(28, 28, 36);
    border-bottom-width: 1px;
    border-bottom-color: rgb(60, 60, 80);
    flex-shrink: 0;
}

.shop-title {
    font-size: 42px;
    color: rgb(220, 220, 230);
    -unity-font-style: bold;
    letter-spacing: 4px;
}

.currency-cluster {
    flex-direction: row;
    align-items: center;
}

.currency-pill {
    flex-direction: row;
    align-items: center;
    background-color: rgb(18, 18, 22);
    border-width: 1px;
    border-color: rgb(60, 60, 80);
    border-radius: 30px;
    padding: 9px 21px;
    margin-left: 15px;
}

.currency-icon {
    font-size: 27px;
    margin-right: 9px;
}

.currency-value {
    font-size: 24px;
    color: rgb(220, 220, 230);
    -unity-font-style: bold;
}

/* ── CATEGORY TAB BAR ── */
.category-scroll {
    background-color: rgb(28, 28, 36);
    border-bottom-width: 1px;
    border-bottom-color: rgb(60, 60, 80);
    height: 96px;
    flex-shrink: 0;
}

.category-tab-row {
    flex-direction: row;
    align-items: center;
    padding: 12px 24px;
    height: 96px;
}

.category-tab {
    background-color: rgba(0, 0, 0, 0);
    border-width: 0;
    border-radius: 30px;
    color: rgb(140, 140, 160);
    font-size: 24px;
    -unity-font-style: bold;
    padding: 12px 33px;
    margin-right: 12px;
    white-space: nowrap;
}

.category-tab:hover {
    background-color: rgba(99, 120, 255, 0.12);
    color: rgb(220, 220, 230);
}

.category-tab.active-cat {
    background-color: rgb(99, 120, 255);
    color: rgb(255, 255, 255);
}

/* ── CONTENT BODY ── */
.shop-body {
    flex-grow: 1;
    position: relative;
    overflow: hidden;
}

/* Same visibility pattern as .tab-screen / .visible in MainLayout */
.content-panel {
    position: absolute;
    left: 0;
    right: 0;
    top: 0;
    bottom: 0;
    display: none;
    padding: 24px;
}

.content-panel.visible {
    display: flex;
}

/* ── SECTION HEADING ── */
.section-heading {
    font-size: 20px;
    color: rgb(140, 140, 160);
    -unity-font-style: bold;
    margin-bottom: 18px;
    margin-top: 6px;
    letter-spacing: 2px;
}

/* ── HERO BANNER ── */
.hero-banner {
    background-color: rgb(40, 44, 80);
    border-width: 1px;
    border-color: rgb(99, 120, 255);
    border-radius: 24px;
    padding: 42px 36px;
    margin-bottom: 30px;
    align-items: flex-start;
    flex-shrink: 0;
}

.hero-eyebrow {
    font-size: 20px;
    color: rgb(99, 120, 255);
    -unity-font-style: bold;
    letter-spacing: 3px;
    margin-bottom: 9px;
}

.hero-title {
    font-size: 42px;
    color: rgb(255, 255, 255);
    -unity-font-style: bold;
    margin-bottom: 12px;
}

.hero-sub {
    font-size: 23px;
    color: rgb(140, 140, 160);
    margin-bottom: 30px;
}

.hero-buy-btn {
    background-color: rgb(99, 120, 255);
    border-width: 0;
    border-radius: 15px;
    color: rgb(255, 255, 255);
    font-size: 24px;
    -unity-font-style: bold;
    padding: 18px 36px;
    align-self: stretch;
}

.hero-buy-btn:hover {
    background-color: rgb(120, 140, 255);
}

/* ── BASE SHOP CARD ── */
.shop-card {
    background-color: rgb(28, 28, 36);
    border-width: 1px;
    border-color: rgb(60, 60, 80);
    border-radius: 21px;
    margin-bottom: 18px;
    overflow: hidden;
    flex-shrink: 0;
}

/* ── FEATURED / BUNDLE CARD (full-width row) ── */
.featured-card {
    flex-direction: row;
    align-items: center;
    padding: 24px;
}

.card-icon-box {
    width: 96px;
    height: 96px;
    background-color: rgb(18, 18, 22);
    border-radius: 15px;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    margin-right: 21px;
}

.card-icon-placeholder {
    font-size: 48px;
}

.card-info {
    flex-grow: 1;
    flex-direction: column;
    margin-right: 18px;
}

.card-name {
    font-size: 24px;
    color: rgb(220, 220, 230);
    -unity-font-style: bold;
    margin-bottom: 6px;
}

.card-desc {
    font-size: 20px;
    color: rgb(140, 140, 160);
    white-space: normal;
}

/* ── PRICE BUTTON ── */
.price-btn {
    background-color: rgb(99, 120, 255);
    border-width: 0;
    border-radius: 15px;
    color: rgb(255, 255, 255);
    font-size: 23px;
    -unity-font-style: bold;
    padding: 15px 24px;
    flex-shrink: 0;
    white-space: nowrap;
}

.price-btn:hover {
    background-color: rgb(120, 140, 255);
}

/* ── GEM GRID (2-column) ── */
.gem-grid {
    flex-direction: row;
    flex-wrap: wrap;
    justify-content: space-between;
}

.gem-card {
    width: 48%;
    flex-direction: column;
    align-items: center;
    padding: 30px 18px 24px 18px;
    margin-bottom: 18px;
}

.gem-icon {
    font-size: 60px;
    margin-bottom: 15px;
}

.gem-card .card-name {
    font-size: 23px;
    margin-bottom: 21px;
    -unity-text-align: middle-center;
}

.gem-card .price-btn {
    align-self: stretch;
    -unity-text-align: middle-center;
}

/* ── OFFER CARD (badge on top, row content below) ── */
.offer-card {
    flex-direction: column;
    padding: 0;
}

.offer-badge {
    background-color: rgb(99, 120, 255);
    color: rgb(255, 255, 255);
    font-size: 18px;
    -unity-font-style: bold;
    padding: 8px 21px;
    letter-spacing: 2px;
    -unity-text-align: middle-center;
}

.offer-badge--urgent {
    background-color: rgb(220, 60, 60);
}

.offer-badge--new {
    background-color: rgb(40, 180, 100);
}

.offer-card-row {
    flex-direction: row;
    align-items: center;
    padding: 18px 24px;
}
